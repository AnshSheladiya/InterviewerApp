const productImagesAngles = ['rear', 'front', 'side', 'top', 'bottom', 'primary'];
const stockStatusOptions = ['In Stock', 'Out of Stock', 'Low Stock'];
const maxRecentSearches = 5;
const maxRelatedProducts = 2;

module.exports = {
  productImagesAngles,
  stockStatusOptions,
  maxRecentSearches,
  maxRelatedProducts,
};
